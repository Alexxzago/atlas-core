import { Router, type Request, type RequestHandler } from "express";
import type { AuthenticationService } from "../identity/services/authenticationService.js";
import type { PlatformAuthorizationService } from "../platformAdmin/services/platformAuthorizationService.js";

function cookie(req:Request,name:string):string|null{for(const part of(req.headers.cookie??"").split(";")){const[key,...rest]=part.trim().split("=");if(key===name)return decodeURIComponent(rest.join("="));}return null;}
export function createPlatformAdminRouter(authentication:AuthenticationService,authorization:PlatformAuthorizationService,controllers:Record<"overview"|"workspaces",RequestHandler>):Router{const router=Router();const admin=(handler:RequestHandler):RequestHandler=>(req,res,next)=>{res.setHeader("Cache-Control","no-store, private");res.setHeader("Pragma","no-cache");try{const raw=cookie(req,authentication.cookieName()),identity=raw?authentication.current(raw):null;if(!identity||!authorization.isPlatformAdministrator(identity.userId as import("../identity/domain/user.js").UserId))throw new Error();handler(req,res,next);}catch{res.status(404).json({error:"Resource not found."});}};router.get("/overview",admin(controllers.overview));router.get("/workspaces",admin(controllers.workspaces));return router;}
