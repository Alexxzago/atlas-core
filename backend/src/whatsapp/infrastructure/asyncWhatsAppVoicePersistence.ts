import type { SqlDatabase } from "../../config/sqlDatabase.js";
import type { ConversationMessage } from "../../conversation/domain/conversation.js";
import type { WorkspaceContext } from "../../types/workspaceContext.js";
import { createHash } from "node:crypto";
import type { AsyncVoiceLookupPort, SynthesisAuthorizationResult, VoiceLease, VoicePolicyMutation, VoicePolicyMutationResult, VoiceWorkSettlement } from "../application/voicePorts.js";
import { voiceBounded, voiceDigest, voiceMode, voicePositive, voiceTimestamp, type AudioTranscriptionRequest, type ConversationAudioTranscript, type VoiceMessageReadModel, type VoicePlayback, type VoiceSynthesisRequest, type WhatsAppOutboundMediaUpload, type WhatsAppVoicePolicy } from "../domain/voice.js";

interface PolicyRow extends Record<string, unknown> { workspace_id:number; company_id:number; whatsapp_connection_id:string; voice_ai_enabled:number; audio_response_mode:"text_only"|"voice_with_text_fallback"; version:number; created_at:string; updated_at:string; }
interface TranscriptRow extends Record<string, unknown> { id:string; workspace_id:number; company_id:number; conversation_id:string; conversation_message_id:string; media_asset_id:string; normalized_transcript:string; language_tag:string|null; input_digest:string; outcome:ConversationAudioTranscript["outcome"]; safe_failure_category:string|null; created_at:string; }
interface MessageRow extends Record<string, unknown> { id:string; conversation_id:string; sender_participant_id:string; direction:"inbound"|"outbound"; content:string; idempotency_key:string|null; execution_record_id:string|null; created_at:string; }
interface ReadRow extends Record<string, unknown> { direction:"inbound"|"outbound"; normalized_transcript:string|null; language_tag:string|null; transcript_outcome:ConversationAudioTranscript["outcome"]|null; transcription_state:string|null; response_policy:string|null; delivery_state:string|null; payload_kind:string|null; synthesis_state:string|null; upload_state:string|null; playback_asset_id:string|null; playback_media_type:string|null; unsupported:number; }
interface WorkRow extends Record<string,unknown>{id:string;workspace_id:number;company_id:number;conversation_id:string;conversation_message_id:string;media_asset_id?:string;outbound_delivery_id?:string;state:AudioTranscriptionRequest["state"];lease_owner:string|null;lease_expires_at:string|null;attempt_count:number;expected_authority_generation:number;safe_outcome:string|null;safe_failure_category:string|null;rendition_settlement_id?:string|null;created_at:string;updated_at:string;completed_at:string|null;}
interface UploadRow extends Record<string,unknown>{id:string;workspace_id:number;company_id:number;outbound_delivery_id:string;media_asset_id:string;provider_media_id:string|null;state:WhatsAppOutboundMediaUpload["state"];lease_owner:string|null;lease_expires_at:string|null;attempt_count:number;safe_error_category:string|null;created_at:string;updated_at:string;}

function policy(row:PolicyRow):WhatsAppVoicePolicy{return Object.freeze({workspaceId:row.workspace_id,companyId:row.company_id,connectionId:row.whatsapp_connection_id,voiceAiEnabled:row.voice_ai_enabled===1,audioResponseMode:row.audio_response_mode,version:row.version,createdAt:row.created_at,updatedAt:row.updated_at});}
function transcript(row:TranscriptRow):ConversationAudioTranscript{return Object.freeze({id:row.id,workspaceId:row.workspace_id,companyId:row.company_id,conversationId:row.conversation_id,messageId:row.conversation_message_id,mediaAssetId:row.media_asset_id,normalizedTranscript:row.normalized_transcript,languageTag:row.language_tag,inputDigest:row.input_digest,outcome:row.outcome,safeFailureCategory:row.safe_failure_category,createdAt:row.created_at});}
function message(row:MessageRow):ConversationMessage{return Object.freeze({id:row.id,conversationId:row.conversation_id,senderParticipantId:row.sender_participant_id,direction:row.direction,content:row.content,idempotencyKey:row.idempotency_key,executionRecordId:row.execution_record_id,createdAt:row.created_at}) as ConversationMessage;}
function work(row:WorkRow):AudioTranscriptionRequest|VoiceSynthesisRequest{return Object.freeze({id:row.id,workspaceId:row.workspace_id,companyId:row.company_id,conversationId:row.conversation_id,messageId:row.conversation_message_id,state:row.state,leaseOwner:row.lease_owner,leaseExpiresAt:row.lease_expires_at,attemptCount:row.attempt_count,expectedAuthorityGeneration:row.expected_authority_generation,safeOutcome:row.safe_outcome,safeFailureCategory:row.safe_failure_category,createdAt:row.created_at,updatedAt:row.updated_at,completedAt:row.completed_at,...(row.media_asset_id===undefined?{outboundDeliveryId:row.outbound_delivery_id!,renditionSettlementId:row.rendition_settlement_id??null}:{mediaAssetId:row.media_asset_id})});}
function upload(row:UploadRow):WhatsAppOutboundMediaUpload{return Object.freeze({id:row.id,workspaceId:row.workspace_id,companyId:row.company_id,outboundDeliveryId:row.outbound_delivery_id,mediaAssetId:row.media_asset_id,providerMediaId:row.provider_media_id,state:row.state,leaseOwner:row.lease_owner,leaseExpiresAt:row.lease_expires_at,attemptCount:row.attempt_count,safeErrorCategory:row.safe_error_category,createdAt:row.created_at,updatedAt:row.updated_at});}

/** Async voice persistence. Transactions retain the synchronous repository's CAS predicates. */
export class AsyncWhatsAppVoicePersistence implements AsyncVoiceLookupPort {
  public constructor(private readonly database:SqlDatabase) {}
  public async findPolicy(context:WorkspaceContext,companyId:number,connectionId:string):Promise<WhatsAppVoicePolicy|null>{const row=(await this.database.query<PolicyRow>("SELECT * FROM whatsapp_voice_policies WHERE workspace_id=? AND company_id=? AND whatsapp_connection_id=?",[context.workspaceId,companyId,connectionId]))[0];return row?policy(row):null;}
  public async findTranscriptByMessage(context:WorkspaceContext,companyId:number,messageId:string):Promise<ConversationAudioTranscript|null>{const row=(await this.database.query<TranscriptRow>("SELECT * FROM conversation_audio_transcripts WHERE workspace_id=? AND company_id=? AND conversation_message_id=?",[context.workspaceId,companyId,messageId]))[0];return row?transcript(row):null;}
  public async isVoiceInboundMessage(context:WorkspaceContext,companyId:number,messageId:string):Promise<boolean>{return (await this.database.query("SELECT 1 FROM audio_transcription_requests WHERE workspace_id=? AND company_id=? AND conversation_message_id=?",[context.workspaceId,companyId,messageId])).length>0;}
  public async isAssistantMessageSemanticallyVisible(context:WorkspaceContext,companyId:number,messageId:string):Promise<boolean>{const rows=await this.database.query<{id:string;response_policy:"standard"|"deferred_voice"}>("SELECT d.id,d.response_policy FROM outbound_deliveries d JOIN provider_message_records r ON r.id=d.provider_message_record_id WHERE r.communication_channel='whatsapp' AND r.direction='outbound' AND r.conversation_message_id=?",[messageId]);if(rows.length===0)return true;for(const row of rows){if(row.response_policy!=="deferred_voice")return true;if((await this.database.query("SELECT 1 FROM voice_response_visibility WHERE workspace_id=? AND company_id=? AND conversation_message_id=? AND outbound_delivery_id=? AND kind='externally_committed'",[context.workspaceId,companyId,messageId,row.id])).length>0)return true;}return false;}
  public async findCompletedInboundTranscriptMessages(context:WorkspaceContext,companyId:number,limit:number):Promise<readonly ConversationMessage[]>{return this.semanticMessages("SELECT m.id,m.conversation_id,m.sender_participant_id,m.direction,t.normalized_transcript AS content,m.idempotency_key,m.assistant_execution_record_id AS execution_record_id,m.created_at FROM conversation_audio_transcripts t JOIN conversation_messages m ON m.id=t.conversation_message_id JOIN conversations cn ON cn.id=m.conversation_id JOIN companies co ON co.id=cn.company_id WHERE t.workspace_id=? AND t.company_id=? AND t.outcome='completed' AND m.direction='inbound' AND co.workspace_id=? AND cn.company_id=? AND NOT EXISTS(SELECT 1 FROM conversation_intelligence_applied_messages a WHERE a.conversation_id=m.conversation_id AND a.conversation_message_id=m.id) ORDER BY t.created_at,t.id LIMIT ?",context,companyId,limit,true);}
  public async findVisibleDeferredAssistantMessages(context:WorkspaceContext,companyId:number,limit:number):Promise<readonly ConversationMessage[]>{return this.semanticMessages("SELECT m.* FROM conversation_messages m JOIN conversations c ON c.id=m.conversation_id JOIN companies co ON co.id=c.company_id JOIN provider_message_records r ON r.conversation_message_id=m.id JOIN outbound_deliveries d ON d.provider_message_record_id=r.id JOIN voice_response_visibility v ON v.outbound_delivery_id=d.id AND v.conversation_message_id=m.id WHERE co.workspace_id=? AND c.company_id=? AND m.direction='outbound' AND r.communication_channel='whatsapp' AND r.direction='outbound' AND d.response_policy='deferred_voice' AND d.state IN ('accepted','delivered','read') AND v.kind='externally_committed' AND NOT EXISTS(SELECT 1 FROM conversation_intelligence_applied_messages a WHERE a.conversation_id=m.conversation_id AND a.conversation_message_id=m.id) ORDER BY m.created_at,m.id LIMIT ?",context,companyId,limit,false);}
  public async recoverableSemanticScopes(limit:number):Promise<readonly {readonly workspaceId:number;readonly companyId:number}[]>{if(!Number.isSafeInteger(limit)||limit<1)return [];const rows=await this.database.query<{workspace_id:number;company_id:number}>("SELECT workspace_id,company_id FROM (SELECT t.workspace_id,t.company_id,t.created_at FROM conversation_audio_transcripts t JOIN conversation_messages m ON m.id=t.conversation_message_id WHERE t.outcome='completed' AND NOT EXISTS(SELECT 1 FROM conversation_intelligence_applied_messages a WHERE a.conversation_id=m.conversation_id AND a.conversation_message_id=m.id) UNION ALL SELECT v.workspace_id,v.company_id,v.created_at FROM voice_response_visibility v JOIN conversation_messages m ON m.id=v.conversation_message_id JOIN provider_message_records r ON r.conversation_message_id=m.id JOIN outbound_deliveries d ON d.id=v.outbound_delivery_id WHERE r.communication_channel='whatsapp' AND r.direction='outbound' AND d.response_policy='deferred_voice' AND d.state IN ('accepted','delivered','read') AND NOT EXISTS(SELECT 1 FROM conversation_intelligence_applied_messages a WHERE a.conversation_id=m.conversation_id AND a.conversation_message_id=m.id)) GROUP BY workspace_id,company_id ORDER BY MIN(created_at),workspace_id,company_id LIMIT ?",[limit]);return Object.freeze(rows.map(row=>Object.freeze({workspaceId:row.workspace_id,companyId:row.company_id})));}
  public async findUploadedProviderMediaId(context:WorkspaceContext,companyId:number,outboundDeliveryId:string):Promise<string|null>{const row=(await this.database.query<{provider_media_id:string}>("SELECT provider_media_id FROM whatsapp_outbound_media_uploads WHERE workspace_id=? AND company_id=? AND outbound_delivery_id=? AND state='uploaded' AND provider_media_id IS NOT NULL",[context.workspaceId,companyId,outboundDeliveryId]))[0];return row?.provider_media_id??null;}
  public async findMessageReadModel(context:WorkspaceContext,companyId:number,conversationId:string,messageId:string):Promise<VoiceMessageReadModel|null>{const row=await this.readRow(context,companyId,conversationId,messageId);if(!row||(row.direction==="inbound"&&row.transcription_state===null&&row.unsupported===0)||(row.direction==="outbound"&&row.response_policy!=="deferred_voice"))return null;const inbound=row.direction==="inbound",deferredState=inbound?null:row.delivery_state==="accepted"||row.delivery_state==="delivered"||row.delivery_state==="read"?row.delivery_state:row.delivery_state==="uncertain"?"uncertain":row.delivery_state==="permanent_failure"?"failed":row.delivery_state==="suppressed"||row.synthesis_state==="suppressed"?"suppressed":row.payload_kind==="text"||row.synthesis_state==="failed"||row.upload_state==="failed"?"fallback":row.payload_kind==="audio"&&row.upload_state==="uploaded"?"audio_ready":"processing",transcriptionState=row.transcript_outcome==="completed"?"completed":row.transcript_outcome==="unsupported"||row.unsupported===1?"unsupported":row.transcription_state==="pending"||row.transcription_state==="leased"||row.transcription_state==="retryable"?"pending":row.transcription_state==="failed"?"failed":row.transcription_state==="suppressed"?"suppressed":null;return Object.freeze({messageId,direction:row.direction,modality:inbound?"audio":"voice",transcript:inbound&&row.transcript_outcome==="completed"?row.normalized_transcript:null,transcriptLanguageTag:inbound&&row.transcript_outcome==="completed"?row.language_tag:null,transcriptionState:inbound?transcriptionState:null,deferredState,fallbackAvailable:!inbound&&(row.payload_kind==="text"||row.synthesis_state==="failed"||row.upload_state==="failed"),playbackAvailable:row.playback_asset_id!==null&&row.playback_media_type!==null});}
  public async findPlayback(context:WorkspaceContext,companyId:number,conversationId:string,messageId:string):Promise<VoicePlayback|null>{const row=await this.readRow(context,companyId,conversationId,messageId);if(!row||(row.direction==="outbound"&&row.response_policy!=="deferred_voice")||row.playback_asset_id===null||row.playback_media_type===null||!(["audio/mpeg","audio/ogg","audio/wav"] as string[]).includes(row.playback_media_type))return null;return Object.freeze({assetId:row.playback_asset_id,mediaType:row.playback_media_type as VoicePlayback["mediaType"]});}
  public async applyPolicy(c:WorkspaceContext,companyId:number,connectionId:string,v:VoicePolicyMutation):Promise<VoicePolicyMutationResult>{voiceBounded(v.actorId,"Voice actor",128);voiceBounded(v.operationId,"Voice operation ID",200);voicePositive(v.expectedVersion,"Voice expected version");voiceMode(v.audioResponseMode);voiceTimestamp(v.occurredAt);const digest=createHash("sha256").update(JSON.stringify({actorId:v.actorId,expectedVersion:v.expectedVersion,voiceAiEnabled:v.voiceAiEnabled,audioResponseMode:v.audioResponseMode})).digest("hex");return this.database.transaction(async d=>{const current=(await d.query<PolicyRow>("SELECT * FROM whatsapp_voice_policies WHERE workspace_id=? AND company_id=? AND whatsapp_connection_id=?",[c.workspaceId,companyId,connectionId]))[0];if(!current)return{kind:"not_found"};const prior=(await d.query<{request_fingerprint:string;outcome:"applied"|"stale_version";resulting_voice_ai_enabled:number|null;resulting_audio_response_mode:PolicyRow["audio_response_mode"]|null;resulting_version:number|null}>("SELECT request_fingerprint,outcome,resulting_voice_ai_enabled,resulting_audio_response_mode,resulting_version FROM whatsapp_voice_policy_operations WHERE workspace_id=? AND company_id=? AND whatsapp_connection_id=? AND operation_id=?",[c.workspaceId,companyId,connectionId,v.operationId]))[0],savedCurrent=policy(current);if(prior){if(prior.request_fingerprint!==digest)return{kind:"replay_mismatch"};const saved=prior.resulting_version===null?null:Object.freeze({...savedCurrent,voiceAiEnabled:prior.resulting_voice_ai_enabled===1,audioResponseMode:prior.resulting_audio_response_mode!,version:prior.resulting_version});return prior.outcome==="applied"?{kind:"replayed_applied",policy:saved!}:{kind:"replayed_stale",policy:saved};}const stale=savedCurrent.version!==v.expectedVersion;if(!stale){const changed=await d.execute("UPDATE whatsapp_voice_policies SET voice_ai_enabled=?,audio_response_mode=?,version=version+1,updated_at=? WHERE workspace_id=? AND company_id=? AND whatsapp_connection_id=? AND version=?",[v.voiceAiEnabled?1:0,v.audioResponseMode,v.occurredAt,c.workspaceId,companyId,connectionId,v.expectedVersion]);if(Number(changed.rowsAffected)!==1)throw new Error("Voice policy CAS lost.");}const saved=policy((await d.query<PolicyRow>("SELECT * FROM whatsapp_voice_policies WHERE workspace_id=? AND company_id=? AND whatsapp_connection_id=?",[c.workspaceId,companyId,connectionId]))[0]!);await d.execute("INSERT INTO whatsapp_voice_policy_operations(workspace_id,company_id,whatsapp_connection_id,operation_id,actor_user_id,request_fingerprint,expected_version,outcome,resulting_voice_ai_enabled,resulting_audio_response_mode,resulting_version,occurred_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)",[c.workspaceId,companyId,connectionId,v.operationId,v.actorId,digest,v.expectedVersion,stale?"stale_version":"applied",saved.voiceAiEnabled?1:0,saved.audioResponseMode,saved.version,v.occurredAt]);return stale?{kind:"stale_version",policy:saved}:{kind:"applied",policy:saved};});}
  public async leaseTranscriptions(c:WorkspaceContext,companyId:number,l:VoiceLease):Promise<readonly AudioTranscriptionRequest[]>{return this.leaseWork("audio_transcription_requests",c,companyId,l) as Promise<readonly AudioTranscriptionRequest[]>;}
  public async leaseSynthesis(c:WorkspaceContext,companyId:number,l:VoiceLease):Promise<readonly VoiceSynthesisRequest[]>{return this.leaseWork("voice_synthesis_requests",c,companyId,l) as Promise<readonly VoiceSynthesisRequest[]>;}
  private async leaseWork(table:"audio_transcription_requests"|"voice_synthesis_requests",c:WorkspaceContext,companyId:number,l:VoiceLease):Promise<readonly (AudioTranscriptionRequest|VoiceSynthesisRequest)[]>{voiceBounded(l.owner,"Lease owner",128);voiceTimestamp(l.now);voiceTimestamp(l.expiresAt);if(!Number.isSafeInteger(l.limit)||l.limit<1)return[];return this.database.transaction(async d=>{const rows=await d.query<WorkRow>(`SELECT * FROM ${table} WHERE workspace_id=? AND company_id=? AND (state IN ('pending','retryable') OR (state='leased' AND lease_expires_at<=?)) ORDER BY created_at,id LIMIT ?`,[c.workspaceId,companyId,l.now,l.limit]),out: (AudioTranscriptionRequest|VoiceSynthesisRequest)[]=[];for(const row of rows){const changed=await d.execute(`UPDATE ${table} SET state='leased',lease_owner=?,lease_expires_at=?,attempt_count=attempt_count+1,updated_at=? WHERE id=? AND workspace_id=? AND company_id=? AND (state IN ('pending','retryable') OR (state='leased' AND lease_expires_at<=?))`,[l.owner,l.expiresAt,l.now,row.id,c.workspaceId,companyId,l.now]);if(Number(changed.rowsAffected)===1){const saved=await this.findWork(d,table,c,companyId,row.conversation_message_id);if(saved)out.push(saved);}}return Object.freeze(out);});}
  public async settleTranscription(c:WorkspaceContext,companyId:number,id:string,owner:string,s:VoiceWorkSettlement):Promise<AudioTranscriptionRequest|null>{return this.settleWork("audio_transcription_requests",c,companyId,id,owner,s) as Promise<AudioTranscriptionRequest|null>;}
  public async settleSynthesis(c:WorkspaceContext,companyId:number,id:string,owner:string,s:VoiceWorkSettlement):Promise<VoiceSynthesisRequest|null>{return this.settleWork("voice_synthesis_requests",c,companyId,id,owner,s) as Promise<VoiceSynthesisRequest|null>;}
  private async settleWork(table:"audio_transcription_requests"|"voice_synthesis_requests",c:WorkspaceContext,companyId:number,id:string,owner:string,s:VoiceWorkSettlement):Promise<AudioTranscriptionRequest|VoiceSynthesisRequest|null>{const complete=s.state==="retryable"?null:s.completedAt,r=await this.database.execute(`UPDATE ${table} SET state=?,lease_owner=NULL,lease_expires_at=NULL,safe_outcome=?,safe_failure_category=?,completed_at=?,updated_at=? WHERE id=? AND workspace_id=? AND company_id=? AND state='leased' AND lease_owner=? AND lease_expires_at>?`,[s.state,s.safeOutcome,s.safeFailureCategory,complete,s.updatedAt,id,c.workspaceId,companyId,owner,s.updatedAt]);if(Number(r.rowsAffected)!==1)return null;const row=(await this.database.query<{conversation_message_id:string}>(`SELECT conversation_message_id FROM ${table} WHERE id=?`,[id]))[0];return row?this.findWork(this.database,table,c,companyId,row.conversation_message_id):null;}
  private async findWork(d:SqlDatabase,table:"audio_transcription_requests"|"voice_synthesis_requests",c:WorkspaceContext,companyId:number,messageId:string):Promise<AudioTranscriptionRequest|VoiceSynthesisRequest|null>{const row=(await d.query<WorkRow>(`SELECT * FROM ${table} WHERE workspace_id=? AND company_id=? AND conversation_message_id=?`,[c.workspaceId,companyId,messageId]))[0];return row?work(row):null;}
  private async semanticMessages(sql:string,context:WorkspaceContext,companyId:number,limit:number,scoped:boolean):Promise<readonly ConversationMessage[]>{if(!Number.isSafeInteger(limit)||limit<1)return [];const rows=await this.database.query<MessageRow>(sql,scoped?[context.workspaceId,companyId,context.workspaceId,companyId,limit]:[context.workspaceId,companyId,limit]);return Object.freeze(rows.map(message));}
  private async readRow(context:WorkspaceContext,companyId:number,conversationId:string,messageId:string):Promise<ReadRow|null>{const sql="SELECT m.direction,t.normalized_transcript,t.language_tag,t.outcome AS transcript_outcome,tr.state AS transcription_state,d.response_policy,d.state AS delivery_state,d.payload_kind,s.state AS synthesis_state,u.state AS upload_state,CASE WHEN m.direction='inbound' THEN (SELECT a.id FROM whatsapp_inbound_media i JOIN media_assets a ON a.id=i.media_asset_id WHERE i.conversation_message_id=m.id AND i.workspace_id=? AND i.company_id=? AND i.provider_kind='audio' AND i.state='associated' AND a.kind='audio' AND a.status='ready' AND a.deleted_at IS NULL LIMIT 1) ELSE (SELECT a.id FROM media_assets a WHERE a.id=d.media_asset_id AND a.kind='audio' AND a.status='ready' AND a.deleted_at IS NULL LIMIT 1) END AS playback_asset_id,CASE WHEN m.direction='inbound' THEN (SELECT a.media_type FROM whatsapp_inbound_media i JOIN media_assets a ON a.id=i.media_asset_id WHERE i.conversation_message_id=m.id AND i.workspace_id=? AND i.company_id=? AND i.provider_kind='audio' AND i.state='associated' AND a.kind='audio' AND a.status='ready' AND a.deleted_at IS NULL LIMIT 1) ELSE (SELECT a.media_type FROM media_assets a WHERE a.id=d.media_asset_id AND a.kind='audio' AND a.status='ready' AND a.deleted_at IS NULL LIMIT 1) END AS playback_media_type,CASE WHEN EXISTS(SELECT 1 FROM whatsapp_inbound_media i JOIN channel_provider_events e ON e.id=i.channel_provider_event_id JOIN channel_execution_requests r ON r.channel_provider_event_id=e.id WHERE i.conversation_message_id=m.id AND i.workspace_id=? AND i.company_id=? AND i.provider_kind='audio' AND r.state='unsupported' AND r.outcome='unsupported') THEN 1 ELSE 0 END AS unsupported FROM conversation_messages m JOIN conversations cn ON cn.id=m.conversation_id JOIN companies co ON co.id=cn.company_id LEFT JOIN conversation_audio_transcripts t ON t.conversation_message_id=m.id AND t.workspace_id=? AND t.company_id=? LEFT JOIN audio_transcription_requests tr ON tr.conversation_message_id=m.id AND tr.workspace_id=? AND tr.company_id=? LEFT JOIN provider_message_records p ON p.conversation_message_id=m.id AND p.direction='outbound' AND p.communication_channel='whatsapp' LEFT JOIN outbound_deliveries d ON d.provider_message_record_id=p.id LEFT JOIN voice_synthesis_requests s ON s.outbound_delivery_id=d.id AND s.workspace_id=? AND s.company_id=? LEFT JOIN whatsapp_outbound_media_uploads u ON u.outbound_delivery_id=d.id AND u.workspace_id=? AND u.company_id=? WHERE m.id=? AND m.conversation_id=? AND cn.company_id=? AND co.workspace_id=?";const row=(await this.database.query<ReadRow>(sql,[context.workspaceId,companyId,context.workspaceId,companyId,context.workspaceId,companyId,context.workspaceId,companyId,context.workspaceId,companyId,context.workspaceId,companyId,messageId,conversationId,companyId,context.workspaceId]))[0];return row??null;}
}
