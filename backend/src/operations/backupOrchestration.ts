import { createHash, randomUUID } from "node:crypto";
import type { BackupObjectStorage } from "./s3BackupObjectStorage.js";

export interface BackupMediaEntry { readonly storageReference:string;readonly digest:string;readonly sizeBytes:number;readonly contentType:string; }
export interface CompleteBackupManifest { readonly formatVersion:1;readonly backupId:string;readonly createdAt:string;readonly applicationBuild:string;readonly migrationHead:string;readonly database:{readonly provider:"turso";readonly mode:"pitr";readonly sourceDatabase:string;readonly recoveryPoint:string};readonly media:{readonly sourceBucket:string;readonly backupBucket:string;readonly objectCount:number;readonly totalBytes:number;readonly inventoryChecksum:string;readonly entries:readonly {readonly stableEntryId:string;readonly originalStorageReference:string;readonly contentType:string;readonly expectedBytes:number;readonly checksum:string;readonly backupObjectKey:string}[]};readonly retention:{readonly retentionDays:number;readonly retentionUntil:string};readonly state:"complete"; }
export interface LiveBackupMedia { inventory():Promise<readonly BackupMediaEntry[]>;read(reference:string,maximumBytes:number):Promise<AsyncIterable<Uint8Array>>; }
export class BackupOrchestrationService { public constructor(private readonly media:LiveBackupMedia,private readonly storage:BackupObjectStorage,private readonly now:()=>Date=()=>new Date()){} public async create(input:{readonly writesQuiesced:boolean;readonly sourceDatabase:string;readonly migrationHead:string;readonly sourceBucket:string;readonly backupBucket:string;readonly applicationBuild:string;readonly retentionDays:number}):Promise<CompleteBackupManifest>{if(!input.writesQuiesced)throw new Error("Writes must be quiesced before creating a backup checkpoint.");if(input.sourceBucket===input.backupBucket||!Number.isSafeInteger(input.retentionDays)||input.retentionDays<1)throw new Error("Backup isolation or retention is invalid.");const backupId=`bkp_${randomUUID().replace(/-/gu,"")}`,createdAt=this.now().toISOString();await this.storage.put(`backup-work/${backupId}/started.json`,bytes("{}"),2,"application/json");try{const entries=await this.media.inventory(),manifestEntries:Array<CompleteBackupManifest["media"]["entries"][number]>=[];for(let index=0;index<entries.length;index+=1){const entry=entries[index]!,stableEntryId=`entry_${String(index).padStart(8,"0")}`,backupObjectKey=`backups/${backupId}/media/${stableEntryId}`;await this.storage.put(backupObjectKey,await this.media.read(entry.storageReference,entry.sizeBytes),entry.sizeBytes,entry.contentType,entry.digest);manifestEntries.push(Object.freeze({stableEntryId,originalStorageReference:entry.storageReference,contentType:entry.contentType,expectedBytes:entry.sizeBytes,checksum:entry.digest,backupObjectKey}));}const inventoryChecksum=checksum(manifestEntries);await this.storage.put(`backup-work/${backupId}/media-complete.json`,bytes("{}"),2,"application/json");const complete=Object.freeze({formatVersion:1,backupId,createdAt,applicationBuild:input.applicationBuild,migrationHead:input.migrationHead,database:{provider:"turso",mode:"pitr",sourceDatabase:input.sourceDatabase,recoveryPoint:createdAt},media:{sourceBucket:input.sourceBucket,backupBucket:input.backupBucket,objectCount:manifestEntries.length,totalBytes:manifestEntries.reduce((sum,item)=>sum+item.expectedBytes,0),inventoryChecksum,entries:manifestEntries},retention:{retentionDays:input.retentionDays,retentionUntil:new Date(this.now().getTime()+input.retentionDays*86400000).toISOString()},state:"complete"} satisfies CompleteBackupManifest);const encoded=JSON.stringify(complete);await this.storage.put(`backups/${backupId}/manifest.json`,bytes(encoded),Buffer.byteLength(encoded),"application/json");return complete;}catch{await this.storage.put(`backup-work/${backupId}/failed.json`,bytes("{}"),2,"application/json").catch(()=>undefined);throw new Error("Backup checkpoint failed.");}} }
function bytes(value:string):AsyncIterable<Uint8Array>{return (async function*(){yield Buffer.from(value);})();}function checksum(entries:readonly CompleteBackupManifest["media"]["entries"][number][]):string{return createHash("sha256").update(entries.map(entry=>`${entry.stableEntryId}:${entry.originalStorageReference}:${entry.expectedBytes}:${entry.checksum}:${entry.backupObjectKey}`).join("\n")).digest("hex");}
