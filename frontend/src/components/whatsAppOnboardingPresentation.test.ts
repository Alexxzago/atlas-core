import assert from "node:assert/strict";
import test from "node:test";
import type { WhatsAppConnectionOperationalStatus } from "../types/api.ts";
import { buildWhatsAppOnboardingViewModel, mapWhatsAppFailureToCustomerMessage } from "./whatsAppOnboardingPresentation.ts";

const value=(overrides:Partial<WhatsAppConnectionOperationalStatus>={}):WhatsAppConnectionOperationalStatus=>({connection:{id:"wac_x",assistantProfileId:"asp_x",phoneNumberId:"123",whatsappBusinessAccountId:"456",status:"inactive",createdAt:"2026-01-01T00:00:00.000Z",updatedAt:"2026-01-01T00:00:00.000Z"},credentialsConfigured:false,validationState:"not_validated",validatedAt:null,validationFailureCode:null,healthState:"inactive",lastProviderActivityAt:null,lastWebhookActivityAt:null,healthFailureCode:null,updatedAt:"2026-01-01T00:00:00.000Z",...overrides});
test("maps each authoritative server state to exactly one current stage",()=>{for(const [status,stage] of [[value(),"credential"],[value({credentialsConfigured:true}),"validation"],[value({credentialsConfigured:true,validationState:"valid",validatedAt:"2026-01-01T00:00:00.000Z"}),"activation"],[value({credentialsConfigured:true,validationState:"valid",validatedAt:"2026-01-01T00:00:00.000Z",connection:{...value().connection,status:"active"}}),"working"]] as const){const model=buildWhatsAppOnboardingViewModel(status);assert.equal(model.currentStage,stage);assert.equal(model.stages.filter(item=>item.state==="current").length,1);}});
test("maps only sanitized failure categories and makes unknown values safe",()=>{assert.equal(mapWhatsAppFailureToCustomerMessage("invalid_credentials"),"credential");assert.equal(mapWhatsAppFailureToCustomerMessage("insufficient_permissions"),"permission");assert.equal(mapWhatsAppFailureToCustomerMessage("future_code"),"unknown");});
