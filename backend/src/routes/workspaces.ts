import{Router,type RequestHandler}from"express";export function createWorkspacesRouter(c:Record<string,RequestHandler>):Router{const r=Router();r.post("/",c.create!);r.get("/",c.list!);r.get("/selected",c.selected!);r.post("/:workspaceId/select",c.select!);r.get("/:workspaceId/memberships",c.memberships!);r.get("/:workspaceId/invitations",c.invitations!);r.post("/:workspaceId/invitations",c.invite!);r.post("/invitations/accept",c.accept!);r.post("/invitations/reject",c.reject!);r.post("/:workspaceId/invitations/:invitationId/revoke",c.revoke!);r.post("/:workspaceId/memberships/:membershipId/role",c.role!);r.post("/:workspaceId/memberships/:membershipId/suspend",c.suspend!);r.post("/:workspaceId/memberships/:membershipId/reactivate",c.reactivate!);r.post("/:workspaceId/memberships/:membershipId/remove",c.remove!);r.post("/:workspaceId/leave",c.leave!);r.post("/:workspaceId/transfer-ownership",c.transfer!);return r;}
