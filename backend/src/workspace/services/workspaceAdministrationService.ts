import{randomUUID}from"node:crypto";import type{Clock}from"../../identity/application/ports.js";import{createEmailAddress,createNormalizedEmail}from"../../identity/domain/email.js";import type{UserId}from"../../identity/domain/user.js";import type{InvitationDeliveryPort,InvitationProofProvider,WorkspaceAdministrationRepositories,WorkspaceAdministrationTransactionPort}from"../application/ports.js";import type{Workspace}from"../../types/workspace.js";import{invitationCurrent,invitationRole,type Invitation}from"../domain/invitation.js";import{LastOwnerPolicy,mayManageMembership,membershipRole,type Membership,type MembershipId,type MembershipRole,type WorkspacePublicId}from"../domain/membership.js";
export class WorkspaceAdministrationError extends Error{}export class WorkspaceAdministrationConflict extends Error{}
export class WorkspaceAdministrationService{private readonly owners=new LastOwnerPolicy();public constructor(private readonly tx:WorkspaceAdministrationTransactionPort,private readonly proofs:InvitationProofProvider,private readonly clock:Clock,private readonly delivery:InvitationDeliveryPort,private readonly invitationOrigin:string,private readonly invitationLifetimeMs=7*24*60*60*1000){}
 public createWorkspace(userId:UserId,nameValue:string){const name=nameValue.trim();if(name.length<2||name.length>100)throw new WorkspaceAdministrationError();return this.tx.execute(r=>{const user=r.users.findById(userId);if(!user||user.status!=="active")throw new WorkspaceAdministrationError();const now=this.clock.now(),publicId=this.id("wsp") as WorkspacePublicId;const workspace=r.workspaces.create({publicId,key:publicId,name});const member=this.membership(workspace.id,userId,"owner",now);r.memberships.create(member);this.owners.assertFinalOwnerCount(r.memberships.countActiveOwners(workspace.id));return{workspace,member};});}
 public listWorkspaces(userId:UserId){return this.tx.execute(r=>r.memberships.listForUser(userId).map(member=>{const workspace=r.workspaces.findById(member.workspaceId);if(!workspace)throw new WorkspaceAdministrationError();return{publicId:workspace.publicId,name:workspace.name,role:member.role};}));}
 public select(userId:UserId,publicId:string){return this.tx.execute(r=>{const workspace=r.workspaces.findByPublicId(publicId as WorkspacePublicId);const member=workspace?r.memberships.findCurrent(userId,workspace.id):null;if(!workspace||!member||member.status!=="active"){r.selections.clear(userId);throw new WorkspaceAdministrationError();}r.selections.save(userId,workspace.id,this.clock.now());return{publicId:workspace.publicId,name:workspace.name,role:member.role};});}
 public selected(userId:UserId){return this.tx.execute(r=>{const id=r.selections.find(userId);if(id===null)return null;const member=r.memberships.findCurrent(userId,id),workspace=r.workspaces.findById(id);if(!member||member.status!=="active"||!workspace){r.selections.clear(userId);return null;}return{publicId:workspace.publicId,name:workspace.name,role:member.role};});}
 public listMemberships(actor:UserId,workspacePublicId:string){return this.withActor(actor,workspacePublicId,(r,_w,a)=>{if(a.role!=="owner"&&a.role!=="administrator")throw new WorkspaceAdministrationError();return r.memberships.listForWorkspace(a.workspaceId).map(m=>({id:m.id,role:m.role,status:m.status,userId:m.userId}));});}
 public listInvitations(actor:UserId,workspacePublicId:string){return this.withActor(actor,workspacePublicId,(r,_w,a)=>{if(a.role!=="owner"&&a.role!=="administrator")throw new WorkspaceAdministrationError();const now=this.clock.now();return r.invitations.listForWorkspace(a.workspaceId).map(i=>{if(i.status==="pending"&&Date.parse(now)>=Date.parse(i.expiresAt)){r.invitations.update({...i,status:"expired",updatedAt:now},i.version);return{id:i.id,recipient:i.recipient,role:i.proposedRole,status:"expired" as const,expiresAt:i.expiresAt};}return{id:i.id,recipient:i.recipient,role:i.proposedRole,status:i.status,expiresAt:i.expiresAt};});});}
 public async invite(actor:UserId,workspacePublicId:string,emailValue:string,roleValue:string):Promise<void>{const email=createNormalizedEmail(createEmailAddress(emailValue)),role=invitationRole(roleValue),proof=this.proofs.create();let issued:{invitation:Invitation;workspaceName:string}|null=null;issued=this.withActor(actor,workspacePublicId,(r,w,a)=>{if(a.role!=="owner"&&a.role!=="administrator")throw new WorkspaceAdministrationError();const now=this.clock.now(),current=r.invitations.findCurrent(w.id,email);if(current&&!r.invitations.update({...current,status:"superseded",supersededAt:now,updatedAt:now},current.version))throw new WorkspaceAdministrationConflict();const invitation:Invitation={id:this.id("inv"),workspaceId:w.id,issuerMembershipId:a.id,issuerUserId:actor,recipient:email,proposedRole:role,purpose:"workspace_invitation",digestVersion:"sha256-v1",proofDigest:proof.digest,status:"pending",deliveryStatus:"pending",version:1,issuedAt:now,expiresAt:new Date(Date.parse(now)+this.invitationLifetimeMs).toISOString(),acceptedAt:null,acceptedByUserId:null,acceptedIp:null,acceptedUserAgent:null,rejectedAt:null,revokedAt:null,supersededAt:null,updatedAt:now};r.invitations.create(invitation);return{invitation,workspaceName:w.name};});const outcome=await this.delivery.deliver({recipient:email,workspaceName:issued.workspaceName,role,acceptanceUrl:`${this.invitationOrigin}/accept-invitation?proof=${encodeURIComponent(proof.raw)}`,expiresAt:issued.invitation.expiresAt,invitationId:issued.invitation.id});this.tx.execute(r=>{r.invitations.setDeliveryStatus(issued!.invitation.id,outcome,this.clock.now());if(outcome==="permanent_failure"){const current=r.invitations.findById(issued!.invitation.id);if(current?.status==="pending")r.invitations.update({...current,status:"revoked",revokedAt:this.clock.now(),updatedAt:this.clock.now()},current.version);}});}
 public accept(userId:UserId,rawProof:string,ip:string,userAgent:string){const proof=this.proofs.parse(rawProof);if(!proof)throw new WorkspaceAdministrationError();return this.tx.execute(r=>{const now=this.clock.now(),user=r.users.findById(userId),invitation=r.invitations.findByDigest(proof.digest);if(!user||user.status!=="active"||!invitation||!invitationCurrent(invitation,now)||!r.workspaces.findById(invitation.workspaceId))throw new WorkspaceAdministrationError();if(!user.authenticationIdentities.some(i=>i.emailVerified&&i.normalizedEmail===invitation.recipient))throw new WorkspaceAdministrationError();if(r.memberships.findCurrent(user.id,invitation.workspaceId))throw new WorkspaceAdministrationConflict();const member=this.membership(invitation.workspaceId,user.id,invitation.proposedRole,now);r.memberships.create(member);if(!r.invitations.update({...invitation,status:"accepted",acceptedAt:now,acceptedByUserId:user.id,acceptedIp:ip.slice(0,128),acceptedUserAgent:userAgent.slice(0,512),updatedAt:now},invitation.version))throw new WorkspaceAdministrationConflict();return member;});}
 public reject(userId:UserId,rawProof:string){const proof=this.proofs.parse(rawProof);if(!proof)throw new WorkspaceAdministrationError();return this.tx.execute(r=>{const now=this.clock.now(),user=r.users.findById(userId),invitation=r.invitations.findByDigest(proof.digest);if(!user||user.status!=="active"||!invitation||!invitationCurrent(invitation,now)||!user.authenticationIdentities.some(i=>i.emailVerified&&i.normalizedEmail===invitation.recipient))throw new WorkspaceAdministrationError();if(!r.invitations.update({...invitation,status:"rejected",rejectedAt:now,updatedAt:now},invitation.version))throw new WorkspaceAdministrationConflict();});}
 public invitationAction(actor:UserId,workspacePublicId:string,invitationId:string,action:"rejected"|"revoked"){return this.withActor(actor,workspacePublicId,(r,w,a)=>{const invitation=r.invitations.findById(invitationId);if(!invitation||invitation.workspaceId!==w.id||invitation.status!=="pending")throw new WorkspaceAdministrationError();if(action==="revoked"&&a.role!=="owner"&&a.role!=="administrator")throw new WorkspaceAdministrationError();if(action==="rejected"&&a.userId!==actor)throw new WorkspaceAdministrationError();const now=this.clock.now();if(!r.invitations.update({...invitation,status:action,rejectedAt:action==="rejected"?now:null,revokedAt:action==="revoked"?now:null,updatedAt:now},invitation.version))throw new WorkspaceAdministrationConflict();});}
 public changeMembership(actor:UserId,workspacePublicId:string,targetId:string,operation:"suspend"|"reactivate"|"remove"|"role",roleValue?:string){return this.withActor(actor,workspacePublicId,(r,w,a)=>{const target=r.memberships.findById(targetId as MembershipId);if(!target||target.workspaceId!==w.id||!mayManageMembership(a,target))throw new WorkspaceAdministrationError();const now=this.clock.now();let role=target.role,status=target.status;if(operation==="role"){role=membershipRole(roleValue??"");if(a.role==="administrator"&&role!=="operator"&&role!=="viewer")throw new WorkspaceAdministrationError();}if(operation==="suspend")status="suspended";if(operation==="reactivate"){if(target.status!=="suspended")throw new WorkspaceAdministrationError();status="active";}if(operation==="remove")status="removed";if(target.status==="removed")throw new WorkspaceAdministrationError();this.owners.assertTransition(target,role,status,r.memberships.countActiveOwners(w.id));const next={...target,role,status,suspendedAt:operation==="suspend"?now:target.suspendedAt,reactivatedAt:operation==="reactivate"?now:target.reactivatedAt,removedAt:operation==="remove"?now:target.removedAt,roleChangedAt:operation==="role"?now:target.roleChangedAt};if(!r.memberships.update(next,target.version))throw new WorkspaceAdministrationConflict();if(status!=="active")r.selections.clear(target.userId,w.id);});}
 public leave(userId:UserId,workspacePublicId:string){return this.withActor(userId,workspacePublicId,(r,w,member)=>{this.owners.assertTransition(member,member.role,"removed",r.memberships.countActiveOwners(w.id));const now=this.clock.now();if(!r.memberships.update({...member,status:"removed",removedAt:now},member.version))throw new WorkspaceAdministrationConflict();r.selections.clear(userId,w.id);});}
 public transfer(actor:UserId,workspacePublicId:string,targetId:string,actorRole?:string){return this.withActor(actor,workspacePublicId,(r,w,a)=>{if(a.role!=="owner")throw new WorkspaceAdministrationError();const target=r.memberships.findById(targetId as MembershipId);if(!target||target.workspaceId!==w.id||target.status!=="active")throw new WorkspaceAdministrationError();const nextActorRole=actorRole?membershipRole(actorRole):"owner";const finalOwners=r.memberships.countActiveOwners(w.id)+(target.role==="owner"?0:1)-(a.role==="owner"&&nextActorRole!=="owner"?1:0);this.owners.assertFinalOwnerCount(finalOwners);const now=this.clock.now();if(target.role!=="owner"&&!r.memberships.update({...target,role:"owner",roleChangedAt:now},target.version))throw new WorkspaceAdministrationConflict();if(nextActorRole!==a.role&&!r.memberships.update({...a,role:nextActorRole,roleChangedAt:now},a.version))throw new WorkspaceAdministrationConflict();});}
 private withActor<T>(userId:UserId,publicId:string,operation:(r:WorkspaceAdministrationRepositories,w:Workspace,m:Membership)=>T):T{return this.tx.execute(r=>{const user=r.users.findById(userId),workspace=r.workspaces.findByPublicId(publicId as WorkspacePublicId);if(!user||user.status!=="active"||!workspace)throw new WorkspaceAdministrationError();const member=r.memberships.findCurrent(userId,workspace.id);if(!member||member.status!=="active")throw new WorkspaceAdministrationError();return operation(r,workspace,member);});}
 private membership(workspaceId:number,userId:UserId,role:MembershipRole,now:string):Membership{return{id:this.id("mem") as MembershipId,workspaceId,userId,role,status:"active",version:1,createdAt:now,activatedAt:now,suspendedAt:null,reactivatedAt:null,removedAt:null,roleChangedAt:null};}private id(prefix:string):string{return`${prefix}_${randomUUID().replaceAll("-","")}`;}}
